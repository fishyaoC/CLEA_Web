@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
	Layout = "~/Views/Shared/_LayoutBackEnd.cshtml";
}
@model Clea_Web.ViewModels.LectorClassViewModel
<div class="main-panel">
	<div class="content-wrapper">
		<div class="row">
			<!--搜尋-->
			<div class="col-12 mb-3">
				<div class="accordion" id="accordionPanelsStayOpenExample">
					@* <div class="accordion-item">
					<h2 class="accordion-header" id="panelsStayOpen-headingOne">
					<button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseOne" aria-expanded="true" aria-controls="panelsStayOpen-collapseOne">
					<h4 class="m-0">搜尋條件</h4>
					</button>
					</h2>
					<div id="panelsStayOpen-collapseOne" class="accordion-collapse collapse show" aria-labelledby="panelsStayOpen-headingOne">
					<div class="accordion-body">
					<form class="forms-sample" method="post" action="@Url.Action("Index","B_AssignBook")">
					@Html.AntiForgeryToken()
					<div class="row">
					<div class="col-md-6">
					<div class="form-group">
					<label for="exampleInputName1">教材代碼</label>
					@Html.TextBoxFor(x => x.schBookItem.B_ID, new { @class = "form-control form-control", placeholder = "教材代碼" })
					</div>
					</div>
					<div class="col-md-6">
					<div class="form-group">
					<label for="exampleInputEmail3">教材名稱</label>
					@Html.TextBoxFor(x => x.schBookItem.B_Name, new { @class = "form-control form-control", placeholder = "教材名稱" })
					</div>
					</div>
					</div>
					<div class="d-flex">
					<button class="btn btn-light mr-2" onclick="">清除條件</button>
					<button type="submit" class="btn btn-primary mr-2">搜尋</button>
					</div>
					</form>
					</div>
					</div>
					</div> *@
					<!--搜尋End-->
					<!--動作-->
					<div class="mb-2 mt-2">
					</div>
					<!--動作End-->
					<!--資料Table-->
					<div class="col-lg-12 grid-margin stretch-card">
						<div class="card">
							<div class="card-body">
								<h4 class="card-title">我的課程資訊</h4>
								@*<p class="card-description">
								Add class <code>.table</code>
								</p> *@
								<div class="table-responsive">
									<table class="table">
										<thead>
											<tr>
												<th>序</th>
												<th>課程名稱</th>
												<th>科目名稱</th>
												<th>檔案上傳</th>
												<th>操作</th>
											</tr>
										</thead>
										<tbody>
											@if (!(Model.classMenuPageList is null) && Model.classMenuPageList.Count > 0)
											{
												Int32 rowCount = 0;
												foreach (var cMenu in Model.classMenuPageList)
												{
													rowCount++;
													<tr>
														<td>@rowCount</td>
														<td>@cMenu.ClassName</td>
														<td>@cMenu.SubName</td>
														<td>@(cMenu.IsUpload ? "已上傳" : "未上傳")</td>
														<td>
															<a href="@Url.Action("Modify", "P_LectorClass", new { CL_UID = cMenu.CL_UID })" class="btn btn-info">編輯</a>
														</td>
													</tr>
												}
											}
											else
											{
												<tr>
													<td colspan="5" align="center">查無資料!</td>
												</tr>
											}
										</tbody>
									</table>
									<!--換頁模組-->
									@if (!(Model.classMenuPageList is null) && Model.classMenuPageList.Count > 0)
									{
										@Html.Partial("../Shared/_pageList", Model.classMenuPageList)
									}
								</div>
							</div>
						</div>
					</div>
					<!--資料Table End-->
				</div>
			</div>
		</div>
	</div>
</div>
<script type="text/javascript">
	function resetItem() {

	}
</script>